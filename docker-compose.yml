services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: RentingDB
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - carrenting_pgdata:/var/lib/postgresql/data

volumes:
  carrenting_pgdata:
